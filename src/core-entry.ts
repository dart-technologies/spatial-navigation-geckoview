/**
 * Subpath bundle entry: `@dart-technologies/spatial-navigation-geckoview/core`
 *
 * Re-exports the algorithmic core (scoring, geometry, focus groups, config,
 * state) for consumers who want to embed the engine without the GeckoView
 * content-script wiring.
 */

export * from '../core/index';
