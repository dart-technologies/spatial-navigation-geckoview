/**
 * Subpath bundle entry: `@dart-technologies/spatial-navigation-geckoview/messaging`
 *
 * Re-exports the messaging adapters (GeckoView, Noop) and factory for
 * consumers who want to talk to the native bridge without pulling in
 * the navigation engine.
 */

export * from '../messaging/index';
